using UnityEngine;
using System.Collections;

public class TheKey : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		Vector3	vc = new Vector3(0.3f,0.3f,0.3f);
		transform.localScale = vc;

	}
}
